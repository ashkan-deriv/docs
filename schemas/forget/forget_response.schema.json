{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Forget Response",
  "description": "Response for forget request, indicating stream cancellation status",
  "type": "object",
  "properties": {
    "forget": {
      "type": "integer",
      "description": "If set to 1, stream exited and stopped. If set to 0, stream did not exist.",
      "enum": [0, 1]
    },
    "echo_req": {
      "type": "object",
      "description": "Echo of the request made."
    },
    "msg_type": {
      "type": "string",
      "description": "Action name of the request made.",
      "enum": ["forget"]
    },
    "req_id": {
      "type": "integer",
      "description": "Optional field sent in request to map to response, present only when request contains `req_id`."
    }
  },
  "required": ["forget", "echo_req", "msg_type"],
  "additionalProperties": false
}

