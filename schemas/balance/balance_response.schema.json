{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Balance Response",
  "description": "Response for balance request, containing current balance of account",
  "type": "object",
  "properties": {
    "balance": {
      "type": "object",
      "description": "Current balance of one or more accounts.",
      "properties": {
        "balance": {
          "type": "number",
          "description": "Balance of current account."
        },
        "currency": {
          "type": "string",
          "description": "Currency of current account."
        },
        "id": {
          "type": "string",
          "description": "A per-connection unique identifier. Can be passed to the `forget` API call to unsubscribe."
        },
        "loginid": {
          "type": "string",
          "description": "Client loginid."
        }
      },
      "required": ["balance", "currency", "loginid"]
    },
    "echo_req": {
      "type": "object",
      "description": "Echo of the request made."
    },
    "msg_type": {
      "type": "string",
      "description": "Action name of the request made.",
      "enum": ["balance"]
    },
    "req_id": {
      "type": "integer",
      "description": "Optional field sent in request to map to response, present only when request contains `req_id`."
    },
    "subscription": {
      "type": "object",
      "description": "For subscription requests only.",
      "properties": {
        "id": {
          "type": "string",
          "description": "A per-connection unique identifier. Can be passed to the `forget` API call to unsubscribe."
        }
      },
      "required": ["id"]
    }
  },
  "required": ["balance", "echo_req", "msg_type"],
  "additionalProperties": false
}

